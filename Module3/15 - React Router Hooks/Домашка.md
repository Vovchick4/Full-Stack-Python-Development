### Завдання 1
Створіть React Router для інтернет-магазину: / (головна), /products (список), /products/:id (деталі), /cart (кошик), /about. NavLink з підсвіткою активної сторінки.

### Завдання 2
Створіть кастомний хук useLocalStorage(key, initialValue), який зберігає та зчитує стан з localStorage. Використайте його в TodoList.

### Завдання 3
Створіть форму додавання товару з валідацією: назва (обов'язково), ціна (>0), категорія (select), опис (textarea). Додайте тест для форми (перевірка рендеру полів).
