### Завдання 1
Створіть клас `Product`: constructor(title, price), метод getPriceWithVAT(vat=20). Створіть клас `DiscountedProduct extends Product`: constructor з додатковим полем discount, перевизначте getPriceWithVAT з урахуванням знижки.

### Завдання 2
Додайте приватне поле #rating до класу Product. Додайте геттер/сеттер. Валідація: rating від 1 до 5. Додайте статичний метод compare(p1, p2), який порівнює ціни.

### Завдання 3
Створіть EventEmitter: методи on(event, callback), emit(event, data), off(event, callback). Протестуйте: підпишіться на подію 'order:created', створіть замовлення, викличте emit.
