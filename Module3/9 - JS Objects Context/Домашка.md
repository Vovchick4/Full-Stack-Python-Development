### Завдання 1
Створіть об'єкт product: title, price, quantity, methods: getTotal() (price * quantity), applyDiscount(percent). Додайте нове поле category через деструктуризацію.

### Завдання 2
Створіть лічильник через замикання: createCounter(start, step). Повертає об'єкт з методами up(), down(), reset(), getValue().

### Завдання 3
Створіть функцію `mergeObjects(...objs)`, яка зливає будь-яку кількість об'єктів через spread. Напишіть функцію рекурсивного фібоначчі.
